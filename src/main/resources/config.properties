#host=<serverIp>:<port>
#host=192.168.100.2:8111 # comment this variable for correct CI configuration
# Everytime local app is restarted, the superUserToken value is generated once again.
# And as of now it seems the new value should be put below every time.
#superUserToken=6246877181826771900 # comment this variable for correct CI configuration
browser=chrome
remote=http://localhost:4444/wd/hub
browserSize=1920x1080
